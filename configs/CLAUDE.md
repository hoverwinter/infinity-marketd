# configs/

Local/runtime configuration. `config.sample.yaml` is the starting point for a real local config (the Makefile defaults to `CONFIG=configs/config.yaml`, e.g. `make serve`).

Do not commit real credentials. For a shareable annotated template, see `examples/config.example.yaml`. Schema and precedence are defined in `internal/config`.
