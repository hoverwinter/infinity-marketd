# examples/

Committed example/template files. `config.example.yaml` documents every configuration key as a starting point for `--config`.

Keep this in sync with `internal/config` when keys change. Use placeholder credentials only; real local config belongs in `configs/`.
