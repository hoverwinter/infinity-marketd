# docs/

Human-facing documentation (some in Chinese).

- `infinity-marketd-project-brief.md` — project positioning and scope.
- `api/` — querier HTTP API reference (`/api/v1`).
- `storage/` — authoritative ClickHouse schema and partitioning rationale.
- `tdx-data/` — TDX binary format references that the `internal/tdx` parsers implement.

These are reference docs, not specs — proposed changes live in `openspec/`.
