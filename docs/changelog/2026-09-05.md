# 2026-09-05

## Market Review V1

- 新增六张复盘表及 Go 写入模型；事件事实不包含来源、版本、更新时间、名称或跨行涨跌幅。
- 新增四个 marketd 导入命令：旧快照、最终事件订正、规范表现指数、规范市场宽度；支持 dry-run，复用 ops，不依赖 Python。
- 新增七个只读 HTTP 接口，支持单日复盘、单股历史原因和按交易日重建昨日样本表现；专用表现指数独立于个股 relay。
- 严格处理同键冲突、显式百分数单位、旧“平板”状态和未知盘口值。订正为整行 upsert；不支持删除、缺字段 patch 或 HTTP 写接口。
- 修复 TDX HQ 多条 K 线解码循环中 pos 被短变量声明遮蔽的问题，补普通证券/指数连续记录测试。880005 可解码连续记录，但指标字段映射未确认，未启用自动宽度写入。
- 对近期 112 份和历史 2594 份真实快照完成全量 dry-run，无解析丢行；未执行生产 ClickHouse 迁移。更新 schema、API、实施边界和上层切换文档。
- 后续补齐 THS Go 在线三池刷新、三种 TDX 专用指数导入、区间 relay/题材查询时重建和复盘矩阵 API；尚未接入 infinity-quant 页面、Excel 导出或 HTTP 写接口。

## Market Review Completion

- `refresh-limit-review`：完整分页和实际日期校验；多日多板沿前一交易日池回溯严格连续板数；实时 dry-run 验证 2026-09-04 的 98 条事件和 1 条摘要。未获取的大面/高位断板/强题材计数为 NULL，不再默认为零。
- `import-limit-performance-tdx`：核对目录名称后采集 880863 昨日涨停、880812 昨日连板、880751 昨日跌停。880864 是昨日振荡，不误映射；非 ST 行情代码未确认，拒绝替代。历史覆盖不足明确告警，连板序列本次仅返回至 2018-06-05。
- 区间 relay/题材查询限制 93 个自然日，订正后重新分组、排名、筛选再分页。新增 `/api/v1/limit-review-matrix`，按股票分页，保留缺失信息，不生成假行情。
- 真实 ClickHouse 隔离测试验证六表、同键替换、nullable 字段、单日和跨日订正、矩阵读取；仅使用新测试库，保留测试数据，无生产事实写入及破坏性操作。
- TDX 官方函数说明证实 880005/880006 的 ADVANCE/DECLINE 为正负 0..3% 分档，不是总涨跌家数。完整 >7% 等字段对照仍待真实软件截图/导出验证，未启用猜测性宽度写入。

## Workbench Integration

- 现有 infinity-console 新增可选 `POST /api/console/imports/limit-review-corrections`：环境令牌鉴权、默认 dry-run、4 MiB 上限、单进程串行及执行超时；Go 文件导入和 HTTP 共享完整行校验，普通 querier 不开放写入口。
- 已在新建隔离 ClickHouse 库完成 HTTP 预览、实际写入、同键订正、FINAL 回读和任务记录验证；测试库保留，未修改生产数据。
- Infinity/quman 新增只读复盘矩阵页和 Excel 导出，复用已有 gateway、marketd 客户端及 openpyxl。旧复盘页面与数据目录未切换，行情缩略图和历史名称仍未加入。

## Production Review Migration

- 正式迁移 `infinity_market`：2,594 个日期、184,868 条涨跌停/炸板事件、2,594 条摘要、10,231 条接力和 560 条题材行；112 个重叠日期只取近期快照，旧文件和排除文件均归档。
- 三种已验证专用表现指数正式入库 5,630 条日线；880005 宽度和非 ST 指数仍未导入。原始历史行情、原因和炸板缺口未宣称补齐。
- Go 迁移增加显式 snapshot-kind，已确认的旧占位 0 转 NULL，原始快照警告进入 ops；查询重建不把历史未知封板率转换成 100%。
- 全量事件字段和逐日摘要通过 HTTP 对账，保留 SHA-256 清单、批次结果、任务回读和审计报告。新增可重复的只读事件审计测试。

## Design Documentation

- 历史材料整合增加 Go `enrich_existing` 保护，材料 HTTP 不再允许无条件 upsert；核心事实替换仅限明确 CLI 授权，旧快照禁止默认覆盖占用事件日期，THS 刷新保留事件归因和未返回的可选值。
- 全量处理 2016-01-04 至 2026-09-04 的 2,594 日材料，156 处明确图内日期重归属；本轮生产补空原因 99 条、主题材 69 条，全字段回读无差异，逻辑事件仍为 184,868 行。
- 产出逐日 HTML 资料册和覆盖/事实核验报告；明确 21 日无材料及 45,542 条初筛候选未完成核图，不将处理完成写成事实完整。详细见 `docs/design/ashare-review-integration-20260905.md`。

- 补充博主材料到市场事实试点记录：抽查 8 日 63 张材料，以既有 Go console 补录接口向生产订正 49 条原因和 45 条题材，全字段回读通过；不新增表/服务或 OCR/Python 依赖。补录前后数据与 ops 任务保留于 Infinity 本地执行报告。
- 明确旧快照基线审计与后续有意订正的区别，禁止用旧快照覆盖已补齐的归因；记录错日期、旧版式、OCR 错位、历史名称、缺失事件和连板问题。

- 补充并收敛 A 股涨跌停复盘数据层设计：加入 2016+ 历史不可见数据的博主复盘恢复方案，P0 不新增 observation/evidence 常驻表，改为通过补录/订正 contract 直接写最终客观事实，来源和订正原因保留在 ops/report。
- 将“昨日涨停表现/昨日非 ST 涨停表现/昨日连板表现”调整为优先采集 THS/TDX 专用表现指数，并新增 `a_share_limit_performance_index_bars_1d` 设计；个股 relay 明细只作为解释和对账。
- 新增 TDX 880005 市场宽度设计，落到 `a_share_market_breadth_daily`，用于保存涨跌家数、涨幅/跌幅大于 3/5/7% 家数，并接入单日复盘和跨日期矩阵。
