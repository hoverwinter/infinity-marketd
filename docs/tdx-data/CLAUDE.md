# docs/tdx-data/

Reference notes on TongDaXin (TDX) data formats (Chinese). These describe the on-disk binary layouts and field meanings that `internal/tdx` decodes.

Treat as the spec when changing parsers. Record formats: `.day`, `.lc1`/`.1`, `.lc5`/`.5` (32-byte records).
